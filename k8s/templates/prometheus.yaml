apiVersion: v1
kind: Service
metadata:
  name: wasmfrp_backend-metrics
  namespace: {{ .Values.namespace }}
  labels:
    app: wasmfrp_backend
    release: k8s-monitoring  # important: matches Prometheus release label
spec:
  selector:
    app: wasmfrp_backend  # must match the Deployment's pod label
  ports:
    - name: metrics
      port: 5000
      targetPort: 5000
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: wasmfrp_backend-monitor
  namespace: {{ .Values.namespace }}
  labels:
    release: k8s-monitoring
spec:
  selector:
    matchLabels:
      app: wasmfrp_backend
  namespaceSelector:
    matchNames:
      - {{ .Values.namespace }}
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
